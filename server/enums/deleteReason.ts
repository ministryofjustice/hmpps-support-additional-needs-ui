/**
 * An enum of reasons a user can supply when deleting a recorded item
 * (challenge, condition, strength, support strategy, or ALN screener).
 */
enum DeleteReason {
  DATA_PROCESSING_OBJECTION = 'DATA_PROCESSING_OBJECTION',
  ENTERED_IN_ERROR = 'ENTERED_IN_ERROR',
}

export default DeleteReason
